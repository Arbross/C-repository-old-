#include <iostream>
#include "Main.h"
#include "Circle.h"
#include "Rectangle.h"
#include "Triangle.h"
using namespace std;

int main()
{
	size_t size = 5;
	Circle circle(size);
	Rectangle(size, size);
	Triangle(size, size, size);

	Main right;





}